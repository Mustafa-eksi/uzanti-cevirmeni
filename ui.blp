using Gtk 4.0;

Window main_window {
    default-height: 500;
    default-width: 1000;
    vexpand: false;
    Box {
        vexpand: false;
        valign: start;
        orientation: horizontal;
        spacing: 10;
        margin-start: 10;
        Label select_label {
            label: "Lütfen bir dosya seçin.";
        }
        Button select_button {
            label: "Dosya seç";
        }
    }
}
